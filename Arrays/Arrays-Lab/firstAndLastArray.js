function solve (arr){
    // Write a function that receives an array of numbers and prints
    //  the sum of the first and last element in that array.
let firstAndLastArr=arr[0] + arr[arr.length-1];
console.log(firstAndLastArr);
}
solve([10, 30, 60 , 90])